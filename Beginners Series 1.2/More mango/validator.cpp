#include "testlib.h"
#include <bits/stdc++.h>
using namespace std;
int main(int argc, char *argv[])
{
    registerValidation(argc, argv);
    long long w = inf.readLong(1LL, 1e9, "w");
    inf.readSpace();
    long long h = inf.readLong(1LL, 1e9, "h");
    inf.readSpace();
    long long n = inf.readLong(1LL, 1e9, "n");
    inf.readEoln();
    inf.readEof();
}
